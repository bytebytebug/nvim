# nvim
nvim config

## Install
```sh
git clone https://github.com/bytebytebug/nvim.git ~/.config/nvim
```

Execute install_deps_arch to get dependencies.
```sh 
./install_deps_arch
```

Execute setup to install tools and plugins
```sh

./setup
```
